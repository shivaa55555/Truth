name: truth_spice
description: A fun Truth or Dare game app with online multiplayer and AI prompts.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # ✅ ADD THESE PACKAGES:
  shared_preferences: ^2.2.2
  firebase_core: ^2.24.0
  firebase_auth: ^4.16.2
  cloud_firestore: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/